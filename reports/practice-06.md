# Отчёт по качеству: триада метрик RAG (практика 6)

> ЗАМЕНИТЕ содержимое разделов своими данными и удалите блоки-подсказки.

## Две метрики поиска

> ЗАМЕНИТЕ. Оба числа приводятся рядом, полными именами и с указанием способа
> получения. Это разные величины, а не одна, измеренная дважды.

| Метрика | Значение | Как получена | С какой практики доступна |
| --- | --- | --- | --- |
| hit-rate@5 |  | механическая сверка поля `source`, `python ingest.py --eval` | П2 |
| Context Precision |  | оценка LLM-судьёй, `python test_suite.py` | П6 |

## Средние по проверочному набору

| Метрика | Среднее по 10 вопросам |
| --- | --- |
| Faithfulness |  |
| Answer Relevance |  |
| Context Precision |  |

## Оценки по вопросам

> ЗАМЕНИТЕ на вывод `test_suite.py` либо на таблицу из десяти строк.

| id | Тип вопроса | F | AR | CP | Комментарий судьи |
| -- | ----------- | - | -- | -- | ----------------- |
|  |  |  |  |  |  |

## Изменения в проверочном наборе

> ЗАМЕНИТЕ. Пять вопросов практики 2 сохраняются. Если какой-то заменён —
> напишите, чем именно он оказался негоден. Замена без объяснения не принимается.
>
> Если объём сужался на практике 5 — перечислите вопросы, переведённые
> в тип `out_of_scope`, и укажите, что набор остался из десяти.

## Разбор галлюцинации

> ЗАМЕНИТЕ. Вопрос, ответ системы, какое утверждение в нём не подтверждается
> найденными фрагментами и почему это произошло.
>
> Если галлюцинаций не найдено — покажите, что их нет: вопросы вне корпуса
> получили отказ, значения Faithfulness высоки по всему набору.

## Сопоставление с техническим заданием

> ЗАМЕНИТЕ. Три метрики, объявленные на практике 1: достигнуты или нет, по числам.

## Ограничения метода

> ЗАМЕНИТЕ. Судьёй выступает языковая модель: она ошибается и склонна завышать
> оценки. Укажите, какая модель судила и совпадает ли она с той, что порождала
> ответы — в этом случае оценка не является независимой.
