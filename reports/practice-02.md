# Протокол замеров: чанкинг и качество поиска (практика 2)

> ЗАМЕНИТЕ содержимое разделов своими данными и удалите блоки-подсказки.
> Этот файл входит в приёмку практики 2: по нему проверяется, что итоговые
> значения выбраны по числу, а не на глаз.

## Исходные данные

> ЗАМЕНИТЕ. Сколько файлов, каких форматов, сколько страниц суммарно.
> Перечислите файлы поимённо — именно эти имена попадают в поле `source`
> и в `expected_sources` проверочного набора.

## Словарь рубрик

> ЗАМЕНИТЕ. Какие значения принимает `category` и по какому правилу
> определяется каждое. Обязательно укажите рубрику по умолчанию.

| Рубрика | По какому правилу присваивается |
| --- | --- |
|  |  |

## Замеры

> ЗАМЕНИТЕ. Не менее двух строк с разными параметрами нарезки.
> Меняйте по одному параметру за прогон, иначе неясно, что подействовало.

| № | CHUNK_SIZE | CHUNK_OVERLAP | Чанков в индексе | hit-rate@5 | hit-rate@1 |
| - | ---------- | ------------- | ---------------- | ---------- | ---------- |
| 1 |  |  |  |  |  |
| 2 |  |  |  |  |  |

> Колонка `hit-rate@1` заполняется, если `hit-rate@5` одинаков во всех прогонах:
> на малом корпусе он упирается в потолок и варианты не различает (§2.6 задания).
> Замер: `from ingest import evaluate_retrieval; evaluate_retrieval(k=1)`.

**Выбранные значения и основание.**

> ЗАМЕНИТЕ. Одна-две фразы: какие значения приняты и по какому числу из таблицы.
> Если ни один показатель не различает варианты, назовите вторичное число,
> по которому сделан выбор, — это тоже результат, но названный.

## Уточнение метрики из технического задания

> ЗАМЕНИТЕ. Какое число было объявлено на практике 1, какое получилось при первом
> замере, и что сделано: число уточнено или оставлено как цель. Уточнение
> делается один раз, здесь.
